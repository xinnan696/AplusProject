<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ucd.urbanflow.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.ucd.urbanflow.domain.pojo.User">
        <id property="id" column="id"/>
        <result property="accountNumber" column="account_number"/>
        <result property="userName" column="user_name"/>
        <result property="department" column="department"/>
        <result property="email" column="email"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="password" column="password"/>
        <result property="role" column="role"/>
        <result property="enabled" column="enabled"/>
        <result property="locked" column="locked"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByAccountNumber" resultMap="UserResultMap">
        SELECT * FROM users
        WHERE account_number = #{accountNumber} AND is_deleted = FALSE
    </select>

    <select id="findByEmail" resultMap="UserResultMap">
        SELECT * FROM users
        WHERE email = #{email}
    </select>

    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users
        WHERE id = #{id} AND is_deleted = FALSE
    </select>

    <select id="findAllActive" resultMap="UserResultMap">
        SELECT * FROM users
        WHERE is_deleted = FALSE
        ORDER BY id ASC
    </select>

    <insert id="save" parameterType="com.ucd.urbanflow.domain.pojo.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (account_number, user_name, department, email, phone_number, password, role, enabled, locked, is_deleted)
        VALUES (#{accountNumber}, #{userName}, #{department}, #{email}, #{phoneNumber}, #{password}, #{role}, #{enabled}, #{locked}, FALSE)
    </insert>

    <update id="update" parameterType="com.ucd.urbanflow.domain.pojo.User">
        UPDATE users
        <set>
            <if test="userName != null and userName != ''">
                user_name = #{userName},
            </if>
            <if test="department != null">
                department = #{department},
            </if>
            <if test="email != null and email != ''">
                email = #{email},
            </if>
            <if test="phoneNumber != null">
                phone_number = #{phoneNumber},
            </if>
            <if test="role != null and role != ''">
                role = #{role},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            enabled = #{enabled},
            locked = #{locked},
            is_deleted = #{isDeleted},
        </set>
        WHERE id = #{id}
    </update>

    <update id="softDeleteById">
        UPDATE users
        SET is_deleted = TRUE
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE users SET password = #{newPassword} WHERE email = #{email}
    </update>

    <update id="updateUserLockedStatus">
        UPDATE users SET locked = #{locked} WHERE email = #{email}
    </update>

</mapper>